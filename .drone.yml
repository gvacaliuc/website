kind: pipeline
name: default

steps:
  - name: build
    image: klakegg/hugo:alpine
    commands:
      - hugo
  - name: publish
    image: plugins/gh-pages
    settings:
      pages_directory: public
      username: gvacaliuc
      password:
        from_secret: gh_password
